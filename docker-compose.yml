version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: cableindex-mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-root}
      - MYSQL_DATABASE=${MYSQL_DATABASE:-cableindex}
      - MYSQL_USER=${MYSQL_USER:-cableindex}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-cableindex}
    volumes:
      - cableindex-mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p${MYSQL_ROOT_PASSWORD:-root}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  cableindex:
    build: .
    container_name: cableindex
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET:-your-secret-key-change-this}
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=${MYSQL_DATABASE:-cableindex}
      - MYSQL_USER=${MYSQL_USER:-cableindex}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-cableindex}
      - MYSQL_SSL=${MYSQL_SSL:-false}
      - UPLOADS_PATH=/app/uploads
    volumes:
      - cableindex-uploads:/app/uploads
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  cableindex-mysql:
  cableindex-uploads: